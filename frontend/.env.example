# API base URL (always localhost from the browser, even when using Docker)
NEXT_PUBLIC_API_URL=http://localhost:3333

# Enable MSW mock API during development (true | false)
NEXT_PUBLIC_ENABLE_MSW=true
